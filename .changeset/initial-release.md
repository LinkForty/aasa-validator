---
"@linkforty/aasa-core": minor
"@linkforty/aasa-worker": minor
"@linkforty/aasa-node": minor
"@linkforty/aasa-widget": minor
"@linkforty/aasa-react": minor
---

Initial release: validate Apple App Site Association (AASA) files for iOS Universal Links.

- `core`: runtime-agnostic validation engine (hosting, format, structure, and identifier checks).
- `worker` / `node`: reference backends exposing `GET /validate`.
- `widget` / `react`: embeddable web component and React component.

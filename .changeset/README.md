# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets).

To record a change for the next release, run `pnpm changeset` and follow the prompts.
Each change creates a markdown file here describing the bump (patch/minor/major) and a summary
that flows into the package CHANGELOGs on release.
